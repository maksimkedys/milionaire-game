export enum ButtonVariant {
  Primary = 'primary',
  Secondary = 'secondary',
}

export enum AppLink {
  Home = '/',
  Game = '/game',
  Result = '/result',
}

export enum AnswerStatus {
  Default = 'default',
  Selected = 'selected',
  Correct = 'correct',
  Wrong = 'wrong',
}

export enum MoneyLevelStatus {
  Default = 'default',
  Current = 'current',
  Passed = 'passed',
}
